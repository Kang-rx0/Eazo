6个corpus，被编码成npz

数据库的corpus chunk有完整段被截断（例如26和27） ---- 分chunk方式？ 检索方式？

# 问题（已解决）

三、自检结果（按严重程度排）
A. 真实的技术缺陷
【最严重】模型调用阻塞整个服务。所有路由写成 async def，但里面的 LLM 调用是同步的（8–10 秒）——这期间整个事件循环卡死，任何人的任何请求（包括静态页、时钟）都得排队。单人演示看不出来，两个人同时用必现"页面卡住"。修法很小：带模型调用的路由改成普通 def，FastAPI 会自动丢进线程池。
/api/clock 没有鉴权，且时钟是全局的。任何未登录的人都能改全系统时间；所有用户共享同一个"今天"。演示道具设计如此，但上线前必须改。
agent_trace 按"虚拟时间"文件名取最新。演示中把时钟往回拨后，侧栏会读到旧轨迹（文件名排序不等于实际生成顺序）。
工具执行没有兜底 try。_exec_tool 里某个工具真抛异常会让整次请求 500，违背"演示不死"原则（目前只有模型调用有兜底）。
RAG 向量有进程内缓存。服务运行中重跑 build_corpus.py，不重启服务的话检索用的还是旧向量。
B. 产品逻辑问题
记忆缺口（你发现的热牛奶问题）——长期偏好没有归宿。
「其实我做了」语义不一致：它现在只是今天重跑的一个提示条件，不会改昨天记录的 feedback——数据层面昨天仍是"没完成"，档位也不回补。有了"改回执"功能后，它理应顺手把昨天那行改成"完成了"。
安全信号纯靠提示词：没有代码级关键词检查，模型哪天漏检"胸口闷"，运动建议就照出。加一个确定性关键词表强制 safety_flag 是廉价的双保险。
同日双击「下班了」的并发窗口：两个请求同时到达可能生成两条当日记录（读取时取最新，基本无害，但数据脏了）。
C. 打磨层（知道即可）
RAG 切块超长段落按 500 字硬切，可能拦腰截断句子，影响个别块的检索质量。
sessions 永不过期、logs/trace/ 无限累积。
上午完成 onboarding 也会生成"今晚"建议（时段语境不敏感）。

# 问题
用户的onboarding这里有分 慢性病/其它慢性病/下列较重情况（只能选择）；能否只问用户“是否有哪些确诊的疾病，例如xxx”，然后一个输入框让用户自己填，我们只在后台进行这些判断。



我们的反馈如下：

1. 最下方的模拟时间应该是可加可减少的。并且不要限制在1小时和1天，可以有多一个量级：10分钟。

2. 每一页都要有返回到上一步的功能（右上角的返回键），直到最后模型给出建议后，用户点击“好，就这样”之后就可以没有返回功能

3. 关于onboarding那里，因为是第一次，希望能够问用户今天经历了什么？（这一块你可以参考一下队友的设计文档和html @docs/V3/DESIGN.md  @docs/V3/prototypes/nightwatch-v0.2.html里的 “这一天身体经历了什么”）。然后这一个要真实给LLM，参与到决策。

4. 还有onboarding这里，用户填自己"平时吃饭主要靠什么"。我希望是四种：大多外卖，主要吃食堂，基本自己做，几样混着来）。同样可以参考 html 0.2版本的。

5. 在LLM给用户建议后，能不能把上分的大字句子里，把关键词标出来，例如“今晚可做最低照顾：吃点热乎的粉面、轻动8分钟、23:00停”，应该吧“最低照顾”标出来，用跟原型里一样的那种黄色字体。

6. 关于”身体不舒服”的按钮，可以出现在”下班了“和用户点击“下班了”的两个页面，如果用户点击了，就要有个框出来让用户写具体什么不舒服，然后再走判断是直接回绝结束还是温和建议。然后后续就不要再有”身体不舒服”的按钮。

7. 关于网页上的提示弹窗：两种类型的弹窗，一个是询问用户是否下班；一个是提醒用户睡觉。

7.1. 关于询问是否下班的弹窗，在用户自己设定的下班时间5分钟后出现，如果点击还没，过半小时再询问，如果还没，就不要再问了。也就是最多只有两次。最少是0次（即用户在提醒前就自主点击下班了）

7.2. 关于睡觉弹窗。是在用户接受了LLM的建议后，应该保留在一个 只显示LLm建议的界面，不可返回。当到了LLM建议的睡觉时间点后，跳出弹窗，提醒用户睡觉。用户点击后，跳到最终一个页面，并给予鼓励性的话语。

8. 衔接上面的睡觉弹窗。用户点击完跳到鼓励界面后，就已经结束了。然后系统自动在凌晨的某一个时间点把页面刷回初始页，即询问用户昨日计划怎么样的（可以参考 html的v0.2版本）

9. 衔接上面。如果用户在很早点击反馈（回答了昨日计划），那么可以跳到home页，即没有中间这些选项框（即没有 “记下了。谢谢你告诉我。” 这一部分的框，等于是中间留空了，但是下发的功能还要在，以及上分的一些固定话语）